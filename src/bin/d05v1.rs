use std::collections::VecDeque;

fn main() {
    let mut lines = include_str!("day05.txt").lines();
    let mut decks = Vec::<VecDeque<char>>::new();
    for line in lines.take(8) {
        for i in (1..35).step_by(4) {
        }
    }
}
